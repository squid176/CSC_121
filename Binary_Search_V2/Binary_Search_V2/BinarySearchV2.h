#pragma once

class BinarySearchV2
{
private:

public:
	void binarySearch(double* userInput, int& min, int& max, double& arrayInput, bool& found, int& midpoint);
	void findMidpoint(int& min, int& max, int& midpoint);
};
