#include "SearchingSorting.h" 

int SearchingSorting::linearSearch(int& value, int* b)
{
	int i = 0;
	bool found = false;
	int foundIndex;
	while (!found)
	{
		if ( *b == value)
		{
			found = true;
			foundIndex = i;
			cout << 
		}
		else
		{
			i++;
		}
	}
}
