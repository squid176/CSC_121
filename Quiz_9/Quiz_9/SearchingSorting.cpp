#include "SearchingSorting.h"

/*void SearchingSorting::linearSearch(int mylist[], int size)
{
	mylist[size];
	int i = 0;
	int value = 5; // this is the number looking for
	bool found = false;
	int foundIndex;
	while (!found)
	{
		if (myList[i] == value)
		{
			found = true;
			foundIndex = i;
		}
		else
		{
			i++;
		}
	}
}*/
